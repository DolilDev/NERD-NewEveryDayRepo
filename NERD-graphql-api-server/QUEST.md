# NERD-graphql-api-server

## Why this task
This task introduces GraphQL, a technology you haven't used based on your profile. Building a GraphQL API server will enhance your skills in designing flexible and efficient data-fetching strategies, a useful aspect in modern web development.

## Instructions
Build a GraphQL API server using Node.js and TypeScript. The project should be structured into several layers: 1. **Core**: Create a main entry point that initializes the server. 2. **Schema**: Define a GraphQL schema using `graphql-tools` or a similar library, including types for users and posts. 3. **Resolvers**: Implement resolvers to handle queries and mutations, ensuring they interact with an in-memory data store (like an array of objects). 4. **Error Handling**: Add error handling for invalid queries and edge cases (e.g., requesting a non-existent user or post). 5. **Testing**: Write unit tests for your resolvers and schema using a testing framework like Jest. 6. **Documentation**: Create a simple README explaining how to run the project, including how to query the API using tools like Postman or GraphQL Playground.

## Add your own twist
Implement authentication for users using JSON Web Tokens (JWT), allowing for protected routes that can only be accessed by authenticated users.

## README (required)
In the repository, create a `README.md` file written **in English**, describing the project:
what it is, how to run it, and what was used (languages, libraries, tools).
This is a mandatory requirement for every quest.

## Completion criteria
- [ ] Setup of a Node.js server with TypeScript support.
- [ ] Creation of a GraphQL schema with at least two types (e.g., User and Post).
- [ ] Implementation of queries and mutations with at least 3 functions each (e.g., getUsers, createUser, updatePost).
- [ ] Error handling for invalid inputs and queries.
- [ ] Unit tests for all resolvers and schema validation.
- [ ] README file with clear setup instructions and example queries.
- [ ] The repository contains a README.md file written in English

---
*Automatically generated by NERD - NewEveryRepoDay.*
