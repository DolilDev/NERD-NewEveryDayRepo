# Real-Time Collaborative Markdown Editor

A real-time collaborative Markdown editor — TypeScript frontend, Python backend,
communicating over Socket.IO.

> 🚧 Work in progress. Full documentation lands in the final step.

## Layout

- `backend/` — Python + FastAPI + python-socketio server.
- `frontend/` — vanilla TypeScript client (bundled with esbuild).
